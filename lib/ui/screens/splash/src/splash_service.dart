import 'package:get/get.dart' hide Trans;
import 'package:riverside/core/utils/stream_subscriber.dart';

class SplashScreenService extends GetxService with StreamSubscriberMixin {
  // SplashScreenService({
  //   FetchChangeEmailBloc? fetchChangeEmailBloc,
  //   AuthService? srv,
  // }) : _srv = srv ?? GetIt.I.get();

  // final AuthService _srv;

  // Stream<Token> get tokenStream$ => _srv.tokenStream;

  // void fetchToken() => _srv.fetchToken();
}
